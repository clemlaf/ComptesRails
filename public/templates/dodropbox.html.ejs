<select name="<%= na %>" <%= add %>
	class="form-control">
  <% if (z0){ %>
    <option value=""><%= z0 %></option>
  <% } %>
  <% for (var i=0;i<res.length;i++){ var list=res[i]; %>
    <option value=<%=list.id%>
      <% if (sele==list.id){ %>selected<% } %>>
      <%= list.name %>
    </option>
  <% } %>
</select>
